import 'package:flutter/material.dart';

class AnalysicsScreen extends StatelessWidget {
  const AnalysicsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text("Analysics")
      ),
    );
  }
}
